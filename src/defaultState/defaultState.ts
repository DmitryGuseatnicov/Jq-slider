import { State } from 'types/types';

const defaultState: State = {
  min: 0,
  max: 100,
  from: 0,
  to: 100,
  step: 1,
  tip: false,
  range: false,
  progress: false,
  scale: false,
  scaleDestiny: 10,
  horizontal: false,
};

export default defaultState;
